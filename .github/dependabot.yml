# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "maven" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "daily"
    ignore:
      - dependency-name: "io.quarkus.platform:*"
        versions: # for LTS versions see https://github.com/quarkusio/quarkus/wiki/Release-Planning
          - "[3.28,3.33)" #Ignore versions from 3.28 (inclusive) up to 3.33 (exclusive).
          - "[3.34,)" #Ignore versions from 3.34 (inclusive) and higher.
